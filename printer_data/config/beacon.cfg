[beacon]
serial: /dev/serial/by-id/usb-Beacon_Beacon_RevD_6DF97FE6515137474C202020FF0C2B2D-if00
x_offset: 0
y_offset: 34
mesh_main_direction: x
mesh_runs: 2
backlash_comp: 0.5
cal_nozzle_z: 0.0
contact_max_hotend_temperature: 300 # increase to probe at print temps
home_xy_position: 192.5,169.2 # update with your safe position
home_z_hop: 3
home_z_hop_speed: 30
home_xy_move_speed: 800
home_method: contact # use proximity for induction homing
home_method_when_homed: proximity # after initial calibration use induction
home_autocalibrate: unhomed # contact will calibrate beacon on first home
autocal_tolerance: 0.0075
autocal_max_retries: 5

[stepper_z]
endstop_pin: probe:z_virtual_endstop
homing_retract_dist: 0

[probe]
#activate_gcode:
#    {% set PROBE_TEMP = 150 %}
#    {% set MAX_TEMP = PROBE_TEMP + 5 %}
#    {% set ACTUAL_TEMP = printer.extruder.temperature %}
#    {% set TARGET_TEMP = printer.extruder.target %}
#
#    {% if TARGET_TEMP > PROBE_TEMP %}
#        { action_respond_info('Extruder temperature target of %.1fC is too high, lowering to %.1fC' % (TARGET_TEMP, PROBE_TEMP)) }
#        M109 S{ PROBE_TEMP }
#    {% else %}
#        # Temperature target is already low enough, but nozzle may still be too hot.
#        {% if ACTUAL_TEMP > MAX_TEMP %}
#            { action_respond_info('Extruder temperature %.1fC is still too high, waiting until below %.1fC' % (ACTUAL_TEMP, MAX_TEMP)) }
#            TEMPERATURE_WAIT SENSOR=extruder MAXIMUM={ MAX_TEMP }
#        {% endif %}
#    {% endif %}
